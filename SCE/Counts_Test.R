counts_htseq <- read.table(file = 'df_gene_counts_starhtseq.tsv', sep = '\t', header = TRUE)
counts_salmon <- read.table(file = 'df_gene_counts_salmon.tsv', sep = '\t', header = TRUE)
raw <- read.table(file = 'GSE77288_molecules-raw-single-per-sample.tsv', sep = '\t', header = TRUE)


